<div class="url-list">
	<h3>Urly Tiny Links</h3>
	<% current_user.urls.each do |url| %>
	  <p><a href="<%= request.base_url +  '/' + url.shortened_url %>" target="_blank"><%= request.base_url + '/' + url.shortened_url %></a> has been viewed <%= url.visits.count %> times.</p>
  <% end %>
</div>